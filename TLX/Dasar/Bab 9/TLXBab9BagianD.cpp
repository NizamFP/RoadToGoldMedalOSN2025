#include <iostream>
using namespace std;

int main() {
    string s = "Hello, World!";
    cout << s.substr(1);
    if (s[0] == 'a', 'i', 'u', 'e', 'o') {
        cout << "vokal";
    }
    else {
        cout << "konsonan";
    }
}